  <h1>All Tasks</h1>

  <ul>
    <% @tasks.each do |task| %>
      <li>
        <%= link_to task.task_name, task_path(task) %>
      </li>
    <% end %>
  </ul>

  <div class="new-task-link">
    <%= link_to "New Task", new_task_path %>
  </div>
